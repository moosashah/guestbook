# Environment (set to 'development' or 'dev' to disable inactivity timeout)
NODE_ENV=production

# AWS Configuration
AWS_REGION=eu-west-2
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
S3_BUCKET_NAME=guestbook-media-dev

# Service Configuration
PORT=3001

# FFmpeg Configuration (optional, auto-detected by platform)
# Docker/Linux: /usr/bin/ffmpeg (default)
# macOS Homebrew: /opt/homebrew/bin/ffmpeg (default)
# FFMPEG_PATH=/custom/path/to/ffmpeg
